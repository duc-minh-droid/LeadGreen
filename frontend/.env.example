# URL of the Django backend (no trailing slash). Used when demo mode is off.
VITE_BACKEND=http://127.0.0.1:8000

# Set to "true" to run the whole app in the browser with canned data and no backend.
# Demo mode is also switched on automatically when VITE_BACKEND is not set.
VITE_DEMO_MODE=false
